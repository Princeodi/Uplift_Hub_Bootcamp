let numbers = [12, -5, 8, 20, 3]; 
let text = "bootcamp"; 
/**let tempCelsius = 30; 

for (let i = 0; i < numbers.length; i++) {
}

/**Challenge 5: Temperature Converter
Understand:
Input: Celsius value (30).
Output: Fahrenheit value (86).
Plan: Apply the formula: $(Celsius \times \frac{9}{5}) + 32$.Store the result in a variable and print it.
Execute:**/
/**Reflect: This is a sequential algorithm—it runs from top to bottom with no loops or "if" branches.**/
let tempCelsius = 30;
let fahrenheit = (tempCelsius * (9/5)) + 32;
console.log(tempCelsius + "C = " + fahrenheit + "F");